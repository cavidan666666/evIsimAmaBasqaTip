@model IEnumerable<MVCKullaniciKaydi.Models.Kullanicilar>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Kullanici_Adi)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Kullanici_Parolasi)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Kullanici_Emaili)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Tarih)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Kullanici_Adi)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Kullanici_Parolasi)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Kullanici_Emaili)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Tarih)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
